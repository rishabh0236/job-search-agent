# Browser Automation
Use Playwright with isolated browser contexts. Persistent sessions only after explicit user authentication.

Use stable selectors and semantic labels. Save screenshot/page snapshot on failure. Avoid coordinate automation. Never log passwords/cookies/tokens. Prevent duplicate submissions after uncertain network state. Browser automation must respect site terms and technical controls.