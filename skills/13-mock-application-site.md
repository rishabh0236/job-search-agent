# Mock Application Site
Build a local HTML application server before real browser integrations.

Include text fields, dropdowns, radio buttons, checkboxes, file upload, long questions, optional/required fields, validation errors, confirmation page and a CAPTCHA-like stop page.

Test field mapping, unknown-field escalation, uploads, pause/continue, final confirmation and duplicate-submit prevention.