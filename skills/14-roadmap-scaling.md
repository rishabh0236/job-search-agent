# Scaling
Local: single backend, single worker, SQLite, local filesystem, Playwright, small source set.
Next: PostgreSQL, Redis, object storage, ingestion worker, browser worker, LLM queue, observability.
Later: workflow engine, multi-tenant isolation, rate limits, distributed browser workers, model routing, cost controls, analytics warehouse.

Design interfaces now so implementations can be replaced later.